warehouse:
  target: dev
  outputs:
    dev:
      type: clickhouse
      schema: default
      host: { { env_var('CLICKHOUSE_HOST') } }
      port: { { env_var('CLICKHOUSE_PORT') } }
      username: default
      password: "{{ env_var('CLICKHOUSE_PASSWORD') }}"
      secure: true
