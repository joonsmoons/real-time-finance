models:
  - name: fact_stocks_aggregate
    columns:
      - name: stocks_aggregate_key
        test:
          - not_null
          - unique
      - name: ticker
        tests:
          - not_null
      - name: starting_timestamp
        tests:
          - not_null
